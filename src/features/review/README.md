# Person A: report and muscle guide

Move the starter report UI out of `src/App.tsx` into this directory as needed.
Use `AnalysisReport` directly. Handle empty corrections, unassessable clips, and
`source: fixture` visibly. Keep sample findings separate from uploaded footage.

Build correction cards, reference illustrations, and the educational target-muscle
guide. Choose reviewed references for the supported exercise. Label approximate
muscle locations as target areas; camera footage does not measure activation.
